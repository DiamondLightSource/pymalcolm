# Define where to write data
- builtin.defines.string:
    name: config_dir
    value: /tmp

# Create some Blocks
- demo.blocks.motion_block:
    mri: MOTION
    config_dir: $(config_dir)

- demo.blocks.detector_block:
    mri: DETECTOR
    config_dir: $(config_dir)

- demo.blocks.scan_1det_block:
    mri: SCAN
    config_dir: $(config_dir)
    label: Mapping x, y with demo detector

# Add a webserver
- web.blocks.web_server_block:
    mri: WEB
