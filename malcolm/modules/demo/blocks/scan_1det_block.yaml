- builtin.parameters.string:
    name: mri
    description: MRI for created block

- builtin.parameters.string:
    name: config_dir
    description: Where to store saved configs

- builtin.defines.docstring:
    value: |
      Scan Block that controls a detector and motion controller to simulate a
      continuous scan

- scanning.controllers.RunnableController:
    mri: $(mri)
    config_dir: $(config_dir)
    description: $(docstring)

- scanning.parts.DatasetTablePart:
    name: DSET

- scanning.parts.SimultaneousAxesPart:
    value: [x, y]

- scanning.parts.DetectorChildPart:
    name: DET
    mri: DETECTOR
    initial_visibility: True

- demo.parts.MotionChildPart:
    name: MOT
    mri: MOTION
    initial_visibility: True
