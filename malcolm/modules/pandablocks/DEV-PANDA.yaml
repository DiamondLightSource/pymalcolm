# Define a directory to store config in
- builtin.defines.tmp_dir:
    name: config_dir

- pandablocks.blocks.panda_manager_block:
    config_dir: $(config_dir)
    mri: PANDA
    #hostname: localhost
    hostname: 172.23.252.201

- web.blocks.web_server_block:
    mri: WEB
